# Challenge Overview
- **Time** : 14 hours
- **Benifits** : kotlin basic syntax,xml basic layout without JetPack,100% on Android Studio IDE instead of custom env,touched the dependencies and set up about Room database and KSP ( kotlin symbol processor ), dynamic rendering xml with kotlin ( just like javascript )
